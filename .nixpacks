[phases.setup]
nixPkgs = ['nodejs', 'npm']

[phases.install]
cmds = [
  'cd backend && npm install'
]

[phases.build]
cmds = [
  'echo "Backend-only deployment - no build step needed"'
]

[start]
cmd = 'cd backend && npm start' 